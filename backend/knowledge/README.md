# Public Knowledge

Only reviewed public knowledge files belong here.

Current committed public source:

```text
resume.md
```

Do not add private biography data, health information, private contacts, names of colleagues,
friends, managers, or other private/personalized data.

Do not commit these files at this stage:

```text
biography_public.md
projects.md
```

Use ignored local drafts under the repository root `private/knowledge/` if private source notes are
needed. Files in `private/` must not be indexed or committed.
